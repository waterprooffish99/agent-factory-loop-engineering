# Project 7 model-cost record

The scheduled beat runs the deterministic Python worker directly:

```text
/home/waterproof-fish/.local/bin/python3 .claude/skills/morning-brief/scripts/brief.py --run
```

It does not call an AI model.

- Input model tokens per beat: 0
- Output model tokens per beat: 0
- Model cost per beat: $0
- Current weekday cadence: approximately 22 beats per month
- Approximate monthly model cost: $0 × 22 = $0/month

Local machine/runtime costs are outside this model-token calculation.
